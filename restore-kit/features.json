{
  "project": "S688LN system-app restore kit",
  "version": "1.3",
  "features": [
    {
      "id": "scan_readonly",
      "desc": "STEP-1 reports installed/hidden/disabled counts + full lists, writes nothing to device",
      "passes": true,
      "evidence": "STEP-1-SCAN.bat uses only pm list / cmd package list (read-only); syntax validated"
    },
    {
      "id": "restore_hidden_system_apps",
      "desc": "Re-install every uninstalled-for-user stock system app via cmd package install-existing",
      "passes": false,
      "evidence": "ASSUMED-correct: device-side loop over `cmd package list packages -u -s`. On-device run by owner pending."
    },
    {
      "id": "reenable_disabled_apps",
      "desc": "pm enable every disabled package",
      "passes": false,
      "evidence": "ASSUMED-correct: device-side loop over `pm list packages -d`. On-device run by owner pending."
    },
    {
      "id": "magisk_debloat_path",
      "desc": "List Magisk modules and reversibly disable a debloater (root) to unmask apps",
      "passes": false,
      "evidence": "ASSUMED-correct: touch <module>/disable + reboot. Requires owner's Magisk grant; pending."
    },
    {
      "id": "verify_step",
      "desc": "STEP-3 re-scans and reports 0 hidden / 0 disabled vs baseline",
      "passes": true,
      "evidence": "STEP-3-VERIFY.bat read-only diff; syntax validated"
    },
    {
      "id": "ci_builds_and_publishes_kit",
      "desc": "GitHub Actions bundles platform-tools, zips kit, writes SHA256SUMS, publishes a Release",
      "passes": true,
      "evidence": "VERIFIED: run 30333294466 success; release restore-kit-v1.0 published S688LN-restore-kit-v1.0.zip (8.1MB, incl adb.exe) + SHA256SUMS; local sha256 == published sha256 (7b203b3c...)."
    },
    {
      "id": "diagnose_collector",
      "desc": "DIAGNOSE.bat collects full read-only inventory (installed/disabled/hidden/APK-file-gone/Magisk masks) into S688LN-diagnostics.zip for offline analysis",
      "passes": true,
      "evidence": "VERIFIED: owner ran DIAGNOSE.bat on the S688LN 2026-07-28 and returned S688LN-diagnostics.zip; all 15 files present and parseable. (v1.1 MISSING_FILE check had a parser bug -> fixed in v1.2, see diagnose_parser_fix.)"
    },
    {
      "id": "diagnose_parser_fix",
      "desc": "v1.2 DIAGNOSE codePath parse no longer false-positives /data/app/~~base64==/ paths; /product/operator/app included in dir listing",
      "passes": true,
      "evidence": "VERIFIED: fixed suffix-parse (p=${l##*=}; f=${l%=$p}) simulated against all 397 real codePath lines from owner's 07_paths.txt -> 0 bad parses; v1.1 logic produced 89 false positives, 89/89 confirmed as '=='-padding truncations via 07_paths.txt."
    },
    {
      "id": "tailored_restore_custom",
      "desc": "RESTORE-CUSTOM.bat v1.3 restores the exact 23 hidden packages and leaves the 4 factory-disabled packages in stock (disabled) state, reverting v1.2's mistaken iotcard enable",
      "passes": false,
      "evidence": "VERIFIED root cause of v1.2 [2/3] failures: all 4 'disabled' packages appear verbatim in the July-21 factory baseline (recon packages_disabled.txt) \u2014 they were never user-disabled. devicelockcontroller/securitycom/gms.supervision are OS-protected (shell pm enable rejected = correct). v1.3 resets iotcard to factory state with default-state + disable-user fallback. Owner run pending."
    }
  ]
}
